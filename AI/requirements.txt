# torch는 nixpacks.toml에서 CPU 버전으로 별도 설치 (CUDA 방지)
# pip install -r 에서 재설치 방지를 위해 여기서는 제외

# Web Framework
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# LLM
openai>=1.30.0

# Vector DB
chromadb>=0.4.24

# Embedding
sentence-transformers>=2.7.0

# YouTube 자막
youtube-transcript-api>=0.6.2

# PDF
PyPDF2>=3.0.1

# LangChain
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0
numpy>=1.26.0
