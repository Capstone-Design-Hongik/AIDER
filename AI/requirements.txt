# PyTorch CPU (CUDA 버전 방지 - Railway 빌드 크기/시간 절감)
torch --extra-index-url https://download.pytorch.org/whl/cpu

# Web Framework
fastapi>=0.110.0
uvicorn>=0.29.0
python-multipart>=0.0.9

# LLM
openai>=1.30.0            # ✅ gpt-4o-mini (분석) + gpt-4o (최종 조언)

# Vector DB
chromadb>=0.4.24          # ✅ PersistentClient 지원 버전

# Embedding
sentence-transformers>=2.7.0

# YouTube 자막
youtube-transcript-api>=0.6.2

# PDF
PyPDF2>=3.0.1

# LangChain (PDF/YouTube 청킹용)
langchain-core>=0.1.0
langchain-text-splitters>=0.0.1

# Utils
python-dotenv>=1.0.0
pydantic>=2.0.0
numpy>=1.26.0