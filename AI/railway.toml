[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn main_server:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300    # 임베딩 모델 첫 다운로드 시간 고려
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3